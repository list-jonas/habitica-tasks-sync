.git
.gitignore
.venv
venv
__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.idea
.vscode
.pytest_cache
.mypy_cache
.ruff_cache
htmlcov
.coverage
data
config.local.*
*.sqlite
*.sqlite3
*.db
# NOTE: config.yaml + tokens/ are intentionally NOT excluded so that
# build-time file injection (Dokploy "Patches", committed-locally
# setups) works. The Dockerfile bakes them in only if present.
# Keep them out of the git repo via .gitignore — never commit secrets.
README.md
LICENSE
docker-compose.yml
Dockerfile
.dockerignore
tests
# Keep docker-entrypoint.sh available — Dockerfile copies it in.
!docker-entrypoint.sh
